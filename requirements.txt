pandas>=2.0
numpy>=1.24
pytz>=2023.3
scikit-learn>=1.3
matplotlib>=3.7
scipy>=1.10
# Only needed to re-run from the raw captures rather than the derived files:
# sentence-transformers>=2.2
# hdbscan>=0.8.33
# langdetect>=1.0.9
